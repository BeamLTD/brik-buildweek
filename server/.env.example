# Copy this file to `.env` and fill in your key. `.env` is git-ignored — never commit a real key.
# BRIK reads the key server-side only; it is never sent to the browser or embedded in the demo.

OPENAI_API_KEY=sk-your-key-here

# Optional overrides:
# MODEL=gpt-5.6
# PORT=8787
