{
  "name": "brik-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "BRIK live demo server — serves the demos and a real GPT-5.6 Analyst endpoint (Responses API, streamed).",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs"
  },
  "dependencies": {
    "openai": "^4.77.0"
  }
}
